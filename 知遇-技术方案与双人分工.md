# 知遇：技术方案与双人开发分工

> 目标：在 24 小时内做出一个能现场演示的 A2A 社交闭环：用户输入知乎内容或议题 → 系统找到两类值得聊的人 → 双方 Agent 先交流一轮 → 输出“为什么适合聊”、证据锚点和第一句开场白 → 用户选择是否接管真人交流。

## 1. 产品核心与评委爆点

### 一句话定位

**知遇不是帮你找最像的人，而是让 Agent 先替你找到值得聊的人，并替你把第一句话说出来。**

### 两种连接模式

| 模式 | 找什么人 | Agent 要证明什么 | 用户拿到什么 |
|---|---|---|---|
| 正向交流 / 同频 | 目标相近、观点相近或经验互补的人 | 你们为什么能迅速建立共同语境 | 共同点、适合聊的理由、开场问题 |
| 反向碰撞 / 异见 | 在同一议题下立场相反，但有证据、有推理的人 | 这不是无意义争吵，而是可展开的建设性分歧 | 分歧点、双方依据、辩论边界、开场问题 |

### 建议主爆点

> **“AI 不只帮你找到像你的人，还敢把最值得辩的人送到你面前。”**

现场不要只演示一个推荐结果，而是围绕同一个知乎议题并排展示：

1. 一位同频候选人：Agent 先聊出共同目标和互补经验。
2. 一位异见候选人：Agent 先完成一轮有证据的观点交锋。
3. 两边都展示立场光谱、证据锚点、会谈摘要和第一句开场白。
4. 用户选择“同行”或“碰撞”，再由真人接管。

这样能让评委直观看到：产品不是传统相似度推荐，而是**双通道关系发现 + AI 破冰**。

注意措辞应始终是“攻击观点、不攻击人”“鼓励辩论、不鼓励骚扰”，不要把“攻击性”包装成产品价值。

## 2. 48 小时 MVP 范围

### 必须完成

- 预置 3～6 个候选用户档案，每个档案绑定 2～3 条知乎内容。
- 用户输入 1～2 个知乎链接，或直接选择一个预置争议议题。
- 生成用户理解卡：关注议题、当前目标、立场、可分享经验、禁区。
- 同时生成同频候选和异见候选各 1 个。
- 每个候选执行一轮 4～6 轮的 Agent 会谈。
- 生成结构化结果：共同点 / 分歧点 / 证据锚点 / 信息增量 / 冷场 / verdict。
- 生成一条安全、具体、可继续追问的开场草稿。
- 用户点击“接管交流”后展示模拟真人私信入口。

### 明确不做

- 不做真实多用户在线匹配。
- 不做完整知乎登录、爬虫、实时热榜抓取。
- 不做语音、数字人、跨平台协议适配。
- 不做开放式无限聊天。
- 不做复杂社交关系图、积分体系和商业化支付。
- 不让模型直接决定用户是否应该联系某人；模型只提供可解释建议，最终选择权归用户。

## 3. 推荐技术架构

```text
浏览器前端
  ├─ 用户输入 / 理解卡 / 模式选择
  ├─ 双通道候选卡
  ├─ 会谈过程与结果
  └─ 真人接管 / 开场草稿
          │ HTTP JSON
          ▼
轻量 API 层
  ├─ Demo 数据与状态管理
  ├─ 用户模块接口
  └─ Agent 模块接口
          │
          ▼
Agent 会谈服务
  ├─ 内容解析 / 预置内容读取
  ├─ 用户立场与兴趣抽取
  ├─ 候选召回与双模式匹配
  ├─ 同频 / 异见会谈编排
  ├─ 证据锚定与质量评分
  ├─ 安全过滤
  └─ 开场草稿生成
          │
          ▼
JSON 文件或 SQLite
  ├─ profiles
  ├─ content_items
  ├─ topics
  ├─ match_candidates
  ├─ conversations
  └─ audit_events
```

### 技术选型建议

- **前端**：原生 HTML/CSS/JavaScript 或 React，优先沿用现有 HTML 视觉稿，避免引入不必要的构建复杂度。
- **后端**：Node.js + Express/Fastify；如果已有 Python Agent 代码，则使用 FastAPI，但不要同时维护两套后端。
- **模型调用**：统一封装 `llmClient`，业务代码不得直接散落模型 SDK 调用。
- **持久化**：72 小时 Demo 使用 SQLite 或 JSON 文件均可。若需要多人并行演示，优先 SQLite。
- **内容数据**：手工准备知乎链接、标题、摘要、作者和立场标签；热榜使用静态种子，不把网络稳定性放在演示关键路径上。
- **部署**：单体服务即可。前后端可以同仓库、同进程部署，但模块边界必须保持清晰。

## 4. 核心数据模型

### Profile

```json
{
  "id": "profile_001",
  "displayName": "小周",
  "summary": "做过 AI 产品交互改版",
  "interests": ["AI 产品", "交互设计"],
  "stances": [
    {"topicId": "topic_entrance", "position": 0.72, "confidence": 0.86}
  ],
  "shareableExperiences": ["做过一次聊天入口改版"],
  "boundaries": ["不讨论私人联系方式"],
  "evidenceIds": ["content_101", "content_102"]
}
```

### CandidateResult

```json
{
  "id": "candidate_001",
  "profileId": "profile_001",
  "mode": "aligned",
  "fitScore": 0.84,
  "positionGap": 0.12,
  "whyWorthTalking": ["共同关注 AI 产品入口", "经验互补"],
  "evidence": [
    {"contentId": "content_101", "claim": "做过聊天入口改版"}
  ],
  "conversationId": "conversation_001",
  "verdict": "worth_talking",
  "verdictReason": "有共同目标，且在入口取舍上存在可展开的小分歧",
  "openingDraft": "你第一次发现聊天入口不适合用户，是在什么场景？"
}
```

`mode` 固定为：

- `aligned`：同频交流
- `opposed`：反向碰撞

`verdict` 固定为：

- `worth_talking`：值得聊
- `maybe`：可聊可不聊
- `not_recommended`：不建议
- `unsafe`：安全原因，不推荐连接

## 5. API 契约：唯一集成边界

开发者 A 和开发者 B 只通过以下接口协作。B 先提供静态 Mock，A 不必等待 Agent 服务完成。

### `POST /api/understanding-card`

输入用户的知乎链接或预置内容，返回理解卡。

### `POST /api/candidates`

```json
{
  "topicId": "topic_entrance",
  "userProfileId": "profile_user",
  "modes": ["aligned", "opposed"],
  "limitPerMode": 1
}
```

返回：`CandidateResult[]`。前端只依赖字段，不关心召回算法和模型提示词。

### `POST /api/conversations`

```json
{
  "userProfileId": "profile_user",
  "candidateProfileId": "profile_001",
  "mode": "opposed",
  "topicId": "topic_entrance"
}
```

返回会谈 ID 和状态；Demo 可以直接返回完成态，也可以用轮询模拟实时生成。

### `GET /api/conversations/:id`

返回：会谈轮次、证据锚点、质量指标、verdict、安全标记和开场草稿。

### `POST /api/handoff`

用户确认接管后，生成真人交流卡片：双方称呼、共同点 / 分歧点、建议开场、禁区提示。

## 6. Agent 模块设计

### 6.1 用户 Agent

输入：用户链接、用户编辑内容、选择的议题和模式。

输出：

- 当前议题和用户目标。
- 立场区间 `-1 ~ 1`：反对到支持。
- 立场置信度。
- 可分享信息和不可分享信息。
- 适合的连接模式。

### 6.2 候选召回

先用规则和向量相似度做粗召回，再用模型做解释，不要让 LLM 从所有人中自由编造候选：

- 同频：目标 / 议题相同，立场距离小，经验互补优先。
- 异见：议题相同，立场距离大，但证据数量和质量达到阈值。
- 两种模式都排除隐私冲突、禁区冲突、低质量内容和历史安全风险。

### 6.3 会谈编排

每轮必须使用结构化输出：

```json
{
  "message": "...",
  "claim": "...",
  "evidenceIds": ["content_101"],
  "question": "...",
  "attackOnPerson": false,
  "informationGain": true
}
```

规则：

- 最多 6 轮，避免 Demo 超时和 Token 浪费。
- 每轮至少满足“回应观点、给出依据、提出追问”中的两项。
- 连续 2 轮无信息增量，提前结束并降低 verdict。
- 异见模式必须检查人身攻击、歧视、威胁、隐私泄露和诱导骚扰。
- 模型无法找到证据时必须输出“无证据”，禁止补写事实。

### 6.4 观察员与评分

观察员不负责继续聊天，只读取完整会谈并输出：

- `explorableDivergence`：可展开分歧轮次。
- `informationGain`：信息增量轮次。
- `consensus`：共识轮次。
- `stall`：冷场轮次。
- `evidenceCoverage`：有证据锚点的轮次比例。
- `safetyFlags`：安全问题。
- `verdict` 与理由。

建议判定：

- 同频模式：共同目标 + 信息增量 + 至少一条具体开场依据。
- 异见模式：立场差距明显 + 双方有证据 + 可展开分歧 > 0 + 无安全风险。
- 任一模式：连续冷场、证据不足或安全风险命中，则降级或不推荐。

## 7. 双人完全解耦分工

### 开发者 A：用户侧产品模块

**端到端负责：**

- 页面结构、视觉还原和响应式。
- 用户输入、链接导入和理解卡编辑。
- 议题选择和同频 / 异见模式切换。
- 双通道候选卡、立场光谱和证据展示。
- 会谈结果、verdict 和真人接管页。
- Mock API 适配层，确保没有 Agent 服务时页面也可演示。

**不负责：**模型调用、召回算法、会谈提示词、观察员评分。

**交付物：**

- `web/` 或 `src/ui/`
- 页面状态机：`input → understanding → candidates → conversation → handoff`
- `apiClient` 和前端类型定义。
- 一套基于 JSON 的 Mock 数据。

### 开发者 B：Agent 会谈模块

**端到端负责：**

- 预置 Profile、知乎内容和议题数据。
- 内容解析、立场抽取和证据索引。
- 同频 / 异见候选召回。
- Agent 会谈编排、观察员和 verdict。
- 安全过滤、开场草稿和审计日志。
- API 路由及 JSON Schema 校验。

**不负责：**页面布局、CSS、前端交互和视觉动画。

**交付物：**

- `server/` 或 `src/agent/`
- `/api/understanding-card`
- `/api/candidates`
- `/api/conversations`
- `/api/handoff`
- 固定的 Demo Seed 数据和模型 Mock 模式。

### 两人共同约定

- 先定 Schema，再写实现；禁止临时修改字段含义。
- A 只调用 API，B 只保证 API 和 Mock 数据符合 Schema。
- 所有模型输出先经过 Zod/Pydantic 校验，再进入前端。
- 每人独立分支开发，不直接修改对方模块。
- 每天只安排一次 20～30 分钟集成，不进行高频同步。
- 任何跨模块需求先写进 `CONTRACT.md`，不通过口头约定改变接口。

## 8. 验收标准

### 产品验收

- 评委能在 30 秒内理解“找值得聊的人 + AI 替你开场”。
- 同一个议题能同时出现同频和异见两类候选。
- 每个推荐都有理由和证据，而不是只给相似度分数。
- 用户能看到第一句具体开场白，并能一键接管。
- 异见模式明确展示“攻击观点，不攻击人”的边界。

### 技术验收

- 任意模型调用失败时，页面仍能用 Mock 完成演示。
- 结构化输出校验失败时，不把脏数据传给前端。
- 会谈最多 6 轮，单次 Demo 不超过 60 秒。
- 所有推荐结果可追溯到输入内容和证据 ID。
- A、B 两人的模块可以分别启动、测试和提交。
